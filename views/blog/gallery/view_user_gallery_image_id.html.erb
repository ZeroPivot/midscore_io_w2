

<!DOCTYPE html>
<html>
<head>
    <title><%= @user %> - @image['title'] %></title>
</head>
<body>
      <center><form action="<%= "#{domain_name(@r)}/gallery/view/#{@user}/tags/search" %>" method="get">
    <input type="text" id="search_tags" name="search_tags" placeholder="Search tags"> <a href="<%= "#{domain_name(@r)}/gallery/view/#{@user}/tags/search" %>">(🏷️,🏷️,...)</a>
    <input type="submit" value="Search">
    </form></center>
        <br>
    <center><img src="<%= "#{domain_name(@r)}/gallery/#{@user}/resized_#{@image["file"]}" %>" alt="<%= @title %>"></center><br>
    <center><a href="<%= "#{domain_name(@r)}/gallery/view/#{@user}" %>">Back to Gallery</a></center>
    <center><a href="<%= "#{domain_name(@r)}/gallery" %>">Back to Gallery List</a></center>
    <center><a href="<%= "#{domain_name(@r)}/" %>">Back to Main Page</a></center>
    <br />
    <br />
    <center>(Title) <%= @image['title'] %></center>
    <br><br>
    <center>(Description) <%= @image['description'] %></center>
    <br><br>
    <center>(Tags) <%= @image['tags'] %></center>
    <center>(Upload Time) <%= @image['date'] %></center>
    <center>(Views) <%= @image['views'] %></center>
    <center>(Filename/Permalink/Full Resolution) <a href="<%= "#{domain_name(@r)}/gallery/#{@user}/#{@image['file']}" %>"><%= @image['file'] %></a></center>
    <br><br>
    <center><a href="<%= "#{domain_name(@r)}/gallery/edit/#{@user}/id/#{@id}" %>">Edit</a></center>
    <center><a href="<%= "#{domain_name(@r)}/gallery/delete/#{@user}/id/#{@id}" %>">Delete</a></center>
</body>
</html>
